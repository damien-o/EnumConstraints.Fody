namespace AssemblyToProcess.Extensions;

public enum StatusAsByte : byte
{
    One = 1,
    Two = 2,
}

public enum StatusAsShort : short
{
    One = 1,
    Two = 2,
}

public enum StatusAsInt32
{
    One = 1,
    Two = 2,
}

public enum StatusAsInt64 : long
{
    One = 1,
    Two = 2,
}
